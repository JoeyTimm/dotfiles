alias :q="exit"
#I use vim so I am used to doing :q to quit so this just appeals to my muscle memor from vim and lets me use :q for everything

alias sai="sudo apt install"
alias sai="sudo apt-get install"
#I find that following along with class demos and guides results in me downloading a lot of extensions so this saves me some typing

alias l='ls -lah'
#ls -lah is probably my most used command so why not make my life easier

#something from the ultimate BA bashrc file
# Color for manpages in less makes manpages a little easier to read
export LESS_TERMCAP_mb=$'\E[01;31m'
export LESS_TERMCAP_md=$'\E[01;31m'
export LESS_TERMCAP_me=$'\E[0m'
export LESS_TERMCAP_se=$'\E[0m'
export LESS_TERMCAP_so=$'\E[01;44;33m'
export LESS_TERMCAP_ue=$'\E[0m'
export LESS_TERMCAP_us=$'\E[01;32m'
